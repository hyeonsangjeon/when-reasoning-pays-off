# Full runtime dependency set for analysis and Azure experiment reproduction.
#
#     python -m venv .venv && . .venv/bin/activate
#     pip install -r requirements.txt
#
# pyproject.toml is the single dependency source. A plain `pip install .`
# intentionally installs only the core/sample CLI.
.[all]
