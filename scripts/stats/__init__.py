"""Supplementary statistical reporting scripts (revision Phase 2, D-003).

These modules add reporting layers on top of the frozen lab methodology
(``docs/05-methodology.md`` v1.0): bootstrap confidence intervals, effect
sizes, and inter-rater agreement. They never mutate raw run artifacts and
never fabricate data — every figure traces back to committed evidence under
``benchmarks/<name>/``.
"""
