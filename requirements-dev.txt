# Development dependencies: runtime deps + the test/lint toolchain.
#
#     pip install -r requirements-dev.txt
#     pytest -q -m "not adaptive_calibration" batch-runner/tests/
#
# Mirrors [project.optional-dependencies].dev in pyproject.toml.
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
jsonschema>=4.0
