# Development dependencies: every runtime extra plus test/lint tools.
#
#     pip install -r requirements-dev.txt
#     pytest -q -m "not adaptive_calibration" batch-runner/tests/
#
# pyproject.toml remains the single dependency source.
-e .[all,dev]
